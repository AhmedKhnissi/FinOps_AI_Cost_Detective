fastapi==0.139.0
uvicorn==0.49.0
openai==2.45.0
python-dotenv==1.2.2

# ── Persistence (step ⑥ Azure Managed PostgreSQL) ───────────────────────────
# Synchronous driver; the API runs DB calls in a threadpool alongside the
# already-blocking Azure scan and AI steps.
psycopg2-binary==2.9.12

# ── Live progress (step ④ WebSocket) ────────────────────────────────────────
# Starlette's WebSocket transport needs one of these installed.
websockets==16.0.0

# ── Auth (step ① Custom JWT) ────────────────────────────────────────────────
# PyJWT issues/verifies the signed tokens; bcrypt hashes the passwords we
# store in the `users` table. `bcrypt` 5.x exposes the PasswordHasher API.
PyJWT==2.13.0
bcrypt==5.0.0
